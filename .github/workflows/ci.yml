name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  ci:
    # Shared, language-agnostic CI — runs `npm run check`. See PackkitLabs/packkit-actions.
    uses: PackkitLabs/packkit-actions/.github/workflows/generator-ci.yml@v1
