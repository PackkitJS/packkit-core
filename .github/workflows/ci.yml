name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  ci:
    # Shared, language-agnostic CI — runs `npm run check`. See PackkitJS/packkit-actions.
    uses: PackkitJS/packkit-actions/.github/workflows/generator-ci.yml@v1
